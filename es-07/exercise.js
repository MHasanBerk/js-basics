let hello = Number('Ciao');     // number
let age = Boolean(18);       // boolean
let isGraduated = false.toString();     // string

console.log(hello);
console.log(age);
console.log(isGraduated);

//when we call hello variable, it gives NaN. Which means not a number. It's only natural because we are converting a string to a number.