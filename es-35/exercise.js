function canPlay() {
  let sport = " Football";
  
  if (true) {
    var personName = "Cosimo"; // let ==> var
  }

  //OR delete the if block and declare "let personName" inside of the function.
  
  console.log(personName + sport);
}
  
canPlay();

// "let" can't be called outside of the block its declared.