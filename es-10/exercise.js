const firstYearCompleted = true;
const yearsCompleted = 1;

yearsCompleted == firstYearCompleted
yearsCompleted === firstYearCompleted

/* double equals compares values while triple equals compares values and types of the variables. So in this example 
true == 1 returns true because their value are the same
true === 1 returns false because while their value are the same, their type are not the same.
*/